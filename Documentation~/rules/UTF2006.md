# UTF2006 — Property constraints, Ordered.By, and List.Map(...).Property lookup properties that managed code stripping can remove

Detects a property constraint (`Has.Property("ParamName")`, `.With.Property("ParamName")`, `Has.Length`, `Has.Count`, `Has.Message`, `Has.InnerException`), `Is.Ordered.By("Name")`, and `List.Map(...).Property("Name")` whose property is not referenced by NUnit itself. NUnit finds the property with reflection at run time, so the Unity linker sees no reference to it and can remove it from a Player build.

| Item     | Value      |
|----------|------------|
| Category | Assertion  |
| Enabled  | True       |
| Severity | Suggestion |
| CodeFix  | False      |

Message: "'{0}' is looked up by name at run time and managed code stripping can remove it: the test fails in the Player. Read the property directly in the actual value instead."

`{0}` is the property as the name of the type that declares it and the property name, e.g. `FileInfo.Length` or `ArgumentException.ParamName`. The diagnostic is reported at the part of the constraint chain that names the property: the `Property(...)` invocation, the `Length`, `Count`, `Message`, or `InnerException` member access, the `By(...)` invocation, or the `Property(...)` invocation on `List.Map(...)`.

Severity is Suggestion. The test runs and passes in the Editor, and it fails only in a Player test run with managed code stripping enabled. Even at the High stripping level the getter is removed only when nothing else in the build references it, which this rule cannot see (see Notes). An error or a warning would flag code that works for every project that runs its tests only in the Editor. Suggestions appear in the IDE; raise the severity to `warning` (see the end of Notes) to report them in the compiler output as well.

## Other workarounds

Instead of reading the property directly, the following also avoid the failure. The diagnostic remains, because the rule does not see them; suppress it with `#pragma warning disable UTF2006` or `.editorconfig`.

### Exclude the test from Player runs

Exclude the test from Player runs with the [UnityPlatform](https://docs.unity3d.com/Packages/com.unity.test-framework@1.4/manual/reference-attribute-unityplatform.html) attribute (or `#if UNITY_EDITOR`), so that it runs only in the Editor:

```csharp
[Test]
[UnityPlatform(RuntimePlatform.OSXEditor, RuntimePlatform.WindowsEditor, RuntimePlatform.LinuxEditor)]
public void FileLength()
{
    Assert.That(new FileInfo(Path), Has.Length.GreaterThan(128));
}
```

### Preserve the property with a link.xml file

Preserve the property in a [link.xml](https://docs.unity3d.com/Manual/managed-code-stripping-xml-formatting.html) file under `Assets/`, so that the Unity linker keeps it in the Player build:

```xml
<linker>
  <assembly fullname="mscorlib">
    <type fullname="System.IO.FileInfo">
      <property name="Length" />
    </type>
    <type fullname="System.ArgumentException">
      <property name="ParamName" />
    </type>
  </assembly>
</linker>
```

## Motivation

The constraints this rule detects name the property as a string. `PropertyConstraint.ApplyTo` and `PropertyExistsConstraint.ApplyTo` call `Type.GetProperty(name, BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Instance)` on the type of the actual value, and `PropertyConstraint.ApplyTo` reads the value with `PropertyInfo.GetValue`. `ListMapper.Property` uses the same binding flags on the type of each element. `Has.Length`, `Has.Count`, `Has.Message`, and `Has.InnerException` (and the same members on `ConstraintExpression`, e.g. `.With.Message`) are shorthands that call `Property("Length")`, `Property("Count")`, `Property("Message")`, and `Property("InnerException")`. `CollectionOrderedConstraint.Matches` calls `Type.GetProperty(name)` (public properties only) on the type of each element for the property given to `By(...)`.

The Unity linker cannot follow a property name passed as a string to NUnit, so the getter is removed from the Player build unless other code references it. The [Managed Stripping Level](https://docs.unity3d.com/Manual/managed-code-stripping-configure.html) decides what is searched for unused code:

- **Minimal**, the default for the IL2CPP scripting backend, searches only UnityEngine and the .NET class libraries. A getter of a class library type such as `FileInfo.Length` or `ArgumentException.ParamName` is removed when the test is the only code that reads it.
- **Low**, **Medium**, and **High** also search user-written assemblies, so a getter of a type declared in the project or in a package can be removed too.
- **Disabled**, the default for the Mono scripting backend, removes nothing.

The rule targets the High level: a property that High can remove is reported, whichever assembly declares it.

When the property is removed, `GetProperty` returns `null` and the test fails in the Player:

- A property constraint with a further constraint (`Has.Length.GreaterThan(128)`, `.With.Property("ParamName").EqualTo("path")`) throws `ArgumentException` ("Property Length was not found").
- A property existence constraint (`Has.Property("ParamName")` alone) fails the assertion.
- `Is.Ordered.By(...)` throws `NullReferenceException`, because it reads the value without checking the result of `GetProperty`.
- `List.Map(...).Property(...)` throws `ArgumentException` ("{0} does not have a {1} property", with the element and the property name).

Reading the property in the actual value (`Assert.That(new FileInfo(path).Length, Is.GreaterThan(128))`) puts a call to the getter in the test assembly. The linker sees this reference and keeps the getter.

A getter that NUnit itself calls is not reported. At the Minimal level `nunit.framework.dll` is not searched for unused code, so its references keep the class library members they call. At the High level `nunit.framework.dll` is searched too, and such a getter is kept only while the NUnit code that calls it is kept; the rule assumes that it is, to keep the suggestion free of noise. `Exception.Message`, `Exception.InnerException`, `String.Length`, `Array.Length`, and `ICollection.Count` are among these getters, so `Has.Message`, `.With.InnerException`, `Has.Length` on a string or an array, and `Has.Count` on a collection are not reported (see Notes for the full list).

Verified against `nunit.framework.dll` in `com.unity.ext.nunit` 2.0.5 by reading its IL: `PropertyConstraint.ApplyTo`, `PropertyExistsConstraint.ApplyTo`, `CollectionOrderedConstraint.Matches`, and `ListMapper.Property` call `Type.GetProperty` with the name, and `Has.get_Length` and `ConstraintExpression.get_Length` (and their `Count`, `Message`, and `InnerException` counterparts) pass the property name to `Property(string)`. Other APIs of the constraint model use reflection too, but with a name fixed by NUnit rather than given by the test, so they are out of scope:

- `Is.EqualTo` reads `KeyValuePair<TKey, TValue>.Key` and `Value` by name; NUnit also calls both getters directly, so they are kept.
- `Is.EqualTo` looks up `IEquatable<T>.Equals`, and the Unity customization of `Is.EqualTo` looks up `GetInstanceID` on a `UnityEngine.Object`. Both check the `MethodInfo` for `null` and fall back to `Object.Equals`, so a removed method does not fail the test.
- `Is.GreaterThan` and the other comparison constraints look up `CompareTo` only when the value does not implement the non-generic `IComparable`.

`Has.Attribute<T>()` reads custom attributes with `GetCustomAttributes`; whether the linker removes custom attributes was not verified, so it is out of scope too. Unity Test Framework 1.6.0 ships no `link.xml` and no `IUnityLinkerProcessor` that preserves them.

## Bad

```csharp
using System;
using System.IO;
using NUnit.Framework;

public class StrippedPropertyTest
{
    private const string Path = "Assets/data.bin";

    private static void Load(string path) => throw new ArgumentNullException(nameof(path));

    [Test]
    public void FileLength()
    {
        Assert.That(new FileInfo(Path), Has.Length.GreaterThan(128)); // UTF2006: 'FileInfo.Length'
    }

    [Test]
    public void ParamName()
    {
        Assert.That(() => Load(null),
            Throws.TypeOf<ArgumentNullException>().With.Property("ParamName").EqualTo("path")); // UTF2006: 'ArgumentException.ParamName'
    }
}
```

## Good

```csharp
using System;
using System.IO;
using NUnit.Framework;

public class StrippedPropertyTest
{
    private const string Path = "Assets/data.bin";

    private static void Load(string path) => throw new ArgumentNullException(nameof(path));

    [Test]
    public void FileLength()
    {
        Assert.That(new FileInfo(Path).Length, Is.GreaterThan(128));
    }

    [Test]
    public void ParamName()
    {
        var ex = Assert.Throws<ArgumentNullException>(() => Load(null));
        Assert.That(ex.ParamName, Is.EqualTo("path"));
    }

    [Test]
    public void ExceptionMessage()
    {
        // Exception.Message is referenced by NUnit, so the rule does not report it
        Assert.That(() => Load(null), Throws.ArgumentNullException.With.Message.Contains("path"));
    }
}
```

## Notes

- The property is looked up on a target type resolved from the constraint chain:
  - After `Throws.TypeOf<T>()`, `Throws.InstanceOf<T>()`, `Is.TypeOf<T>()`, or `Is.InstanceOf<T>()` followed by `.With` or `.And`, the target type is `T`. After `Throws.Exception`, `Throws.ArgumentException`, `Throws.ArgumentNullException`, `Throws.InvalidOperationException`, and `Throws.TargetInvocationException`, it is that exception type.
  - After a property step that is not yet resolved to a constraint (`Has.Property("A").With.Property("B")` or `Has.Property("A").Property("B")`), it is the type of the property `A`, because `ConstraintExpression.With` is a prefix operator and `B` is read from the value of `A`.
  - After a resolved constraint followed by `.With` or `.And` (`Has.Property("A").EqualTo(1).With.Property("B")`), it is the target type of that constraint, because `Constraint.With` is `Constraint.And` and `B` is read from the same actual value.
  - After `All`, `Some`, `None`, or `Exactly(n)`, and for `Is.Ordered.By(...)` and `.Then.By(...)`, it is the element type of the current target type (the `T` of the `IEnumerable<T>` it implements).
  - Otherwise, it is the static type of the actual value of the `Assert.That` or `Assume.That` invocation that takes the constraint chain as an argument. For `List.Map(collection).Property(...)`, it is the element type of `collection`.
- The property is resolved like the run-time lookup: an instance property of that name, declared on the target type or inherited, public or not for property constraints and `List.Map(...).Property`, and public only for `Is.Ordered.By(...)`.
- The diagnostic is not reported when:
  - The target type cannot be resolved: the static type is `object`, `dynamic`, a type parameter, or `System.Type`, the element type of a non-generic collection is unknown, or the constraint is stored in a variable or returned by a helper method.
  - The property name is not a compile-time constant. A string literal and `nameof(...)` are constants.
  - The target type has no property of that name. The run-time lookup then runs on the run-time type of the actual value, which the rule cannot know, e.g. `Has.Length` on a `FileSystemInfo` variable that holds a `FileInfo`, or `Throws.Exception.With.Property("ParamName")` (`Exception` has no `ParamName`). Write `Throws.ArgumentException` or `Throws.TypeOf<T>()` so that the rule can see the type.
  - The getter of the property is referenced by `nunit.framework.dll`, or overrides or implements such a getter (e.g. `MemoryStream.Length` overrides `Stream.Length`, and `HashSet<T>.Count` implements `ICollection<T>.Count`). The list below is taken from the IL of `nunit.framework.dll` in `com.unity.ext.nunit` 2.0.5. `UnityEngine.TestRunner` also references getters, but it is compiled from the Unity Test Framework package source, so its references change with the package version; they are not in the list. A UnityEngine property is therefore always reported, e.g. `Has.Property("name")` on a `GameObject`, even when the build happens to keep its getter.
- The list is a heuristic at the Low, Medium, and High levels. `nunit.framework.dll` is searched as well, and a listed getter is kept only while the NUnit code that calls it is kept; a test that fails because a listed getter is removed is not reported.
- The rule does not see other references to the getter. A getter that other code in the Player build calls, e.g. `fileInfo.Length` read elsewhere in the test assembly, is kept but still reported. Suppress the diagnostic at that site with `#pragma warning disable UTF2006`.
- `UnityPlatform` attributes, `#if UNITY_EDITOR`, and link.xml files are not exclusion conditions (see Other workarounds). `UnityPlatform` attributes and conditional compilation have many forms, and a link.xml file is not part of the compilation.

<details>
<summary>Property getters of .NET class library types referenced by nunit.framework.dll (com.unity.ext.nunit 2.0.5)</summary>

| Type                                                  | Properties                                                                                                                                                                                                                                                                  |
|-------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `System.AggregateException`                           | `InnerExceptions`                                                                                                                                                                                                                                                           |
| `System.AppDomain`                                    | `CurrentDomain`, `FriendlyName`                                                                                                                                                                                                                                             |
| `System.Array`                                        | `Length`, `Rank`                                                                                                                                                                                                                                                            |
| `System.Collections.DictionaryEntry`                  | `Key`, `Value`                                                                                                                                                                                                                                                              |
| `System.Collections.Generic.Dictionary<TKey, TValue>` | `Count`, `Item`, `Keys`                                                                                                                                                                                                                                                     |
| `System.Collections.Generic.ICollection<T>`           | `Count`                                                                                                                                                                                                                                                                     |
| `System.Collections.Generic.IDictionary<TKey, TValue>`| `Item`, `Keys`                                                                                                                                                                                                                                                              |
| `System.Collections.Generic.IEnumerator<T>`           | `Current`                                                                                                                                                                                                                                                                   |
| `System.Collections.Generic.IList<T>`                 | `Item`                                                                                                                                                                                                                                                                      |
| `System.Collections.Generic.KeyValuePair<TKey, TValue>`| `Key`, `Value`                                                                                                                                                                                                                                                             |
| `System.Collections.Generic.List<T>`                  | `Count`, `Item`                                                                                                                                                                                                                                                             |
| `System.Collections.Generic.Stack<T>`                 | `Count`                                                                                                                                                                                                                                                                     |
| `System.Collections.ICollection`                      | `Count`                                                                                                                                                                                                                                                                     |
| `System.Collections.IDictionary`                      | `Item`, `Keys`, `Values`                                                                                                                                                                                                                                                    |
| `System.Collections.IEnumerator`                      | `Current`                                                                                                                                                                                                                                                                   |
| `System.Collections.IList`                            | `Item`                                                                                                                                                                                                                                                                      |
| `System.Console`                                      | `Error`, `Out`                                                                                                                                                                                                                                                              |
| `System.DateTime`                                     | `Now`, `UtcNow`                                                                                                                                                                                                                                                             |
| `System.DateTimeOffset`                               | `Offset`                                                                                                                                                                                                                                                                    |
| `System.Delegate`                                     | `Method`                                                                                                                                                                                                                                                                    |
| `System.Diagnostics.Debugger`                         | `IsAttached`                                                                                                                                                                                                                                                                |
| `System.Diagnostics.Process`                          | `Id`                                                                                                                                                                                                                                                                        |
| `System.Environment`                                  | `CurrentDirectory`, `Is64BitOperatingSystem`, `MachineName`, `NewLine`, `OSVersion`, `UserDomainName`, `UserName`, `Version`                                                                                                                                                |
| `System.Exception`                                    | `InnerException`, `Message`, `StackTrace`                                                                                                                                                                                                                                   |
| `System.Globalization.CultureInfo`                    | `CurrentCulture`, `CurrentUICulture`, `InvariantCulture`, `Name`, `NumberFormat`, `TwoLetterISOLanguageName`                                                                                                                                                                |
| `System.Globalization.NumberFormatInfo`               | `InvariantInfo`                                                                                                                                                                                                                                                             |
| `System.IO.BinaryReader`                              | `BaseStream`                                                                                                                                                                                                                                                                |
| `System.IO.FileSystemInfo`                            | `Attributes`, `CreationTime`, `Exists`, `FullName`, `LastAccessTime`                                                                                                                                                                                                        |
| `System.IO.Stream`                                    | `CanRead`, `CanSeek`, `Length`, `Position`                                                                                                                                                                                                                                  |
| `System.IO.TextWriter`                                | `Encoding`                                                                                                                                                                                                                                                                  |
| `System.IntPtr`                                       | `Size`                                                                                                                                                                                                                                                                      |
| `System.Lazy<T>`                                      | `Value`                                                                                                                                                                                                                                                                     |
| `System.Nullable<T>`                                  | `HasValue`, `Value`                                                                                                                                                                                                                                                         |
| `System.OperatingSystem`                              | `Platform`, `Version`                                                                                                                                                                                                                                                       |
| `System.Reflection.Assembly`                          | `CodeBase`, `FullName`, `Location`                                                                                                                                                                                                                                          |
| `System.Reflection.AssemblyName`                      | `Version`                                                                                                                                                                                                                                                                   |
| `System.Reflection.FieldInfo`                         | `FieldType`, `IsStatic`                                                                                                                                                                                                                                                     |
| `System.Reflection.MemberInfo`                        | `DeclaringType`, `Name`                                                                                                                                                                                                                                                     |
| `System.Reflection.MethodBase`                        | `ContainsGenericParameters`, `IsAbstract`, `IsFamily`, `IsGenericMethod`, `IsGenericMethodDefinition`, `IsPublic`, `IsStatic`                                                                                                                                               |
| `System.Reflection.MethodInfo`                        | `ReturnType`                                                                                                                                                                                                                                                                |
| `System.Reflection.ParameterInfo`                     | `IsOptional`, `Member`, `ParameterType`                                                                                                                                                                                                                                     |
| `System.Reflection.PropertyInfo`                      | `PropertyType`                                                                                                                                                                                                                                                              |
| `System.String`                                       | `Chars`, `Length`                                                                                                                                                                                                                                                           |
| `System.Text.Encoding`                                | `Default`                                                                                                                                                                                                                                                                   |
| `System.Text.RegularExpressions.Capture`              | `Value`                                                                                                                                                                                                                                                                     |
| `System.Text.StringBuilder`                           | `Length`                                                                                                                                                                                                                                                                    |
| `System.Threading.CountdownEvent`                     | `CurrentCount`                                                                                                                                                                                                                                                              |
| `System.Threading.Thread`                             | `CurrentPrincipal`, `CurrentThread`, `ManagedThreadId`, `ThreadState`                                                                                                                                                                                                       |
| `System.TimeSpan`                                     | `TotalMilliseconds`, `TotalSeconds`                                                                                                                                                                                                                                         |
| `System.Type`                                         | `Assembly`, `BaseType`, `ContainsGenericParameters`, `FullName`, `GenericParameterPosition`, `HasElementType`, `IsAbstract`, `IsArray`, `IsClass`, `IsEnum`, `IsGenericParameter`, `IsGenericType`, `IsGenericTypeDefinition`, `IsSealed`, `Namespace`                      |
| `System.Version`                                      | `Build`, `Major`, `Minor`, `Revision`                                                                                                                                                                                                                                       |
| `System.Xml.XmlNode`                                  | `Attributes`, `ChildNodes`, `FirstChild`, `InnerText`, `Name`, `NodeType`, `Value`                                                                                                                                                                                          |

</details>

To change the severity, add the following to `.editorconfig` or `.globalconfig`:

```editorconfig
dotnet_diagnostic.UTF2006.severity = warning
```
